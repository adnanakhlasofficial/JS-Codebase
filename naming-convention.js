/**
 * Full Name // wrong
 * Full_Name // right // wrong sometimes
 * Full-Name // wrong
 * 
 */

/**
 * 
 * 3 way to name a variable
 * 
 * 1. fullname  == normal case
 * 2. full_name = snake_case
 * 3. fullName = camelCase (recomended)
 * 4. Function = we can't use reserved keyword
 */

var nameFunation = "Hi";